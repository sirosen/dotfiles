# platform-specific binaries (not scripts)
# which I want in this dir but not in git
shfmt

# included git repos and such
testssl.sh

# managed venvs for python tools installed by the managed venvs script
.managed-venvs
pylint
